# Upstream: mattpocock/skills

Apipoj Skills v6 is based on reviewed commit `6654f6b60cd9d5be8b54c6fafe44346dabeb3b76`.

Only promoted engineering and productivity skills are imported. Updates are reviewed, localized into native Thai, verified against the English mirror, and never auto-merged into a release branch.

SPK approval gates, evidence receipts, Thai-first defaults, explicit security checks, runtime tooling, and project-documentation workflows are stricter overlays and win when contracts conflict.

The 2026-09-01 review selectively adopted `wait-what` routing through `CONTEXT-MAP.md` and visible
question separators in `asking`. Existing SPK invocation and YAML gates already cover the matching
upstream fixes. Editorial punctuation sweeps and skills under upstream's `in-progress` bucket remain
excluded from the canonical SPK behavior and default roster.

The 2026-09-21 review selectively ported the PR body template from upstream
`skills/in-progress/pr` at `c55ee46073ed923f86ce59a5eb3b6d895095d1b7` into SPK `pr`.
That upstream skill credits Dex Horthy / HumanLayer `show-me`. The lock pin stays
`6654f6b60cd9d5be8b54c6fafe44346dabeb3b76`. This is not a promotion of `in-progress`
into the default roster.
