# SPK Wiki Log

Append-only chronological record of ingests, queries, lints, and auto-fixes.
Format: <ISO8601> <VERB> <args>
Ingest entries must include `hash=<first 16 hex characters of the source's sha256>` for dedup matching.

(empty — entries appended as the wiki evolves)
